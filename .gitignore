# ==========================================================
# 1. 빌드 및 설치 파일 (PyInstaller, Python)
# ==========================================================
# PyInstaller가 생성하는 폴더 및 파일
dist/
build/
*.spec

# Python 환경 및 캐시
__pycache__/
*.pyc

# 가상 환경 폴더 (일반적인 이름)
venv/
.venv/
env/

# ==========================================================
# 2. 사용자별/운영체제별 파일 (보안 및 노이즈 제거)
# ==========================================================
# API Key가 저장되는 설정 파일 (프로젝트 코드가 아닌 홈 폴더에 생성되지만,
# 혹시 모를 로컬 테스트 폴더 생성을 대비해 명시적으로 제외)
# 경로: ~/.text_processor/config.ini

# macOS 생성 파일
.DS_Store

# Windows Thumbnail Cache
Thumbs.db